eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
    instance:
      preferIpAddress: true

spring:
  application:
    name: customer1

server:
  port: 0 # Use a random unassigned HTTP port. To scan for a free port (using OS natives to prevent clashes) use server.port=0